import { ObjectForPipe } from './object-for.pipe';

describe('ObjectForPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectForPipe();
    expect(pipe).toBeTruthy();
  });
});
